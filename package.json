{
  "name": "pacquet-workspace",
  "private": true,
  "scripts": {
    "build": "cargo build --release --bin pacquet"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.0.8",
      "onFail": "download"
    }
  }
}
